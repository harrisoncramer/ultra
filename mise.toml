[tools]
go = "1.25.1"
golangci-lint = "v2.7.0"
actionlint = "latest"
vhs = "0.11.0"

[env]
# Install `go install`ed binaries (ultra itself) to a stable, always-on-PATH
# location during local development, instead of mise's version-scoped go bindir.
GOBIN = "{{env.HOME}}/go/bin"
