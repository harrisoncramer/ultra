# ultra

The `ultra` package takes an opinionated, Go-native approach toward secret management with docker and docker compose. 

It handles the injection of secrets into containers automatically. Define your configuration once, and let Ultra do the rest.

NOTE: This project is in Alpha and subject to breaking changes.
